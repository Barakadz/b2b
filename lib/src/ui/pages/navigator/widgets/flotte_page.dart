
import 'package:flutter/material.dart';

class FlotteWidget extends StatelessWidget {

  const FlotteWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is Flotte'),
      ),
    );
  }
}